# Використовуємо легку версію Python
FROM python:3.9-slim

# Робоча папка всередині контейнера
WORKDIR /app

# Встановлюємо Celery (і бібліотеку для пошти, вона вбудована, але для певності)
RUN pip install celery

# Копіюємо наші файли в контейнер
COPY . .

# Команду запуску ми пропишемо в docker-compose, тому тут нічого не запускаємо